import { teamProjection } from "./projects";
export const newsQuery = `*[_type == "newsArticle" && defined(slug.current) && defined(publishedAt) && dateTime(publishedAt) <= dateTime(now())] | order(publishedAt desc) {_id,"slug":slug.current,title,"excerpt":coalesce(excerpt,""),"category":coalesce(categories[0],"News"),"image":coverImage,"content":coalesce(content,[]),"author":author->${teamProjection},publishedAt,seo}`;
